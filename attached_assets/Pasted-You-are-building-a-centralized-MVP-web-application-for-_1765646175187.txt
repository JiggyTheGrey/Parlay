You are building a **centralized MVP web application** for **team-vs-team crypto wagering** for the game *Bloodstrike*.This is **NOT** a play-to-earn platform and **does NOT use smart contracts**.The platform is fully custodial (like Stake.com).All balances are controlled by the backend.---## 🎯 PRODUCT GOALAllow two teams to:* Create team accounts* Bet against each other using crypto* Play a Bloodstrike match off-platform* Manually confirm the winner* Have the platform release funds to the winning team---## ❗ STRICT CONSTRAINTS* NO smart contracts* NO public bettors* ONLY team vs team* ONLY captains can initiate matches & withdrawals* Withdrawal requires **50% team member approval*** Keep everything MVP-simple---## 🧱 TECH STACK (USE THIS EXACTLY)* Frontend: Next.js + Tailwind* Backend: Node.js + Express* Database: * Auth: Username + password* Blockchain: Somnia (custodial wallets)* Crypto lib: ethers.js---## 🧩 CORE DATA MODELS### UserFields:* id* username* passwordHash* teamId* role (`captain` | `member`)---### TeamFields:* id* name* captainUserId* walletAddress* encryptedPrivateKey* walletBalance---### MatchFields:* id* teamAId* teamBId* betAmount* status (`pending` | `accepted` | `completed` | `disputed`)* teamASubmittedWinner (boolean)* teamBSubmittedWinner (boolean)* winnerTeamId---### WithdrawalRequestFields:* id* teamId* amount* approvalsCount* status (`pending` | `approved` | `completed`)* createdAt---### WithdrawalApprovalFields:* id* withdrawalRequestId* userId---## 🔁 REQUIRED FLOWS---### 1️⃣ Team Creation* User creates a team* User becomes captain* Platform generates custodial wallet (server-side)* Wallet balance starts at 0---### 2️⃣ Team Members* Captain can invite members* Members cannot move funds* Members CAN approve withdrawals---### 3️⃣ Match Creation* Captain creates a match* Select opponent team* both teams must input the same amount before bet can be placed successfully* Set bet amount* Match status = `pending`---### 4️⃣ Match Acceptance* Opponent captain accepts* Bet amount is locked from both teams* Match status = `accepted`---### 5️⃣ Match Result Submission (IMPORTANT)* Each captain submits winner separately* If both captains submit the SAME winner:  * Match settles  * Winning team receives pot* If captains submit DIFFERENT winners:  * Match status = `disputed`  * Admin can manually resolve---### 6️⃣ Withdrawal Request (IMPORTANT RULE)* ONLY captain can initiate withdrawal* Withdrawal request status = `pending`* Team members must approve the withdrawal#### Approval Logic:* Every team member can click “Approve Withdrawal”* Approval is stored in `WithdrawalApproval`* Once **50% or more of team members approve**:  * Withdrawal is processed automatically  * Funds are sent on-chain  * Status becomes `completed`⚠️ Members do NOT need to approve at the same time.---## 🖥️ REQUIRED PAGES### Auth* Login* Signup### Dashboard* Team wallet balance* Active matches* Withdrawal requests### Match Page* Match info* Submit winner (captain only)### Withdrawal Page* Request withdrawal (captain)* Approve withdrawal (members)---## 🔐 SECURITY REQUIREMENTS* Encrypt private keys at rest* Prevent double approvals* Prevent withdrawal if balance insufficient* Log every balance change---## 🛠️ BUILD ORDER1. Database schema2. Auth system3. Team + wallet logic4. Match system5. Result confirmation6. Withdrawal approval system7. Basic UI---## 🚫 DO NOT BUILD* Odds calculation* External bettors* Game API integration* Automation* Mobile app---## ✅ FINAL INSTRUCTIONBuild a **working MVP**, not a perfect system.Favor clarity and simplicity over optimization.